logging.level.org.hibernate.SQL=DEBUG

job.parentSummary.fixed.delay.seconds=900
job.parentSummary.fixed.initial.seconds=900

spring.datasource.data=classpath:data.sql, classpath:populateDatabase.sql
